# This rule is not used by the project tenant-controller itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to platform.viper6z.dev resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: tenant-controller
    app.kubernetes.io/managed-by: kustomize
  name: tenant-viewer-role
rules:
- apiGroups:
  - platform.viper6z.dev
  resources:
  - tenants
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - platform.viper6z.dev
  resources:
  - tenants/status
  verbs:
  - get
